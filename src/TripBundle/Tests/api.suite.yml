actor: ApiTester

modules:
    enabled:
        - Cli
        -   REST:
                url: /
                depends:
                    - Symfony
        -   Doctrine2:
                depends: Symfony
                cleanup: true
        -   Symfony:
                app_path: '../'
                environment: 'test'
        - \Helper\Api
