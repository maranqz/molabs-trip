# NOTE: This file is auto generated by the openapi generator program.
# https://github.com/openapitools/openapi-generator
# Do not edit the class manually.

parameters:
    trip.serializer: 'TripBundle\Service\JmsSerializer'
    trip.validator: 'TripBundle\Service\SymfonyValidator'

services:
    _defaults:
        autowire: true
        autoconfigure: true

    TripBundle\:
        resource: '../../'
        exclude:
            - '../../Entity/'
            - '../../Migrations/'
            - '../../Tests/'

    TripBundle\Normalizer\EntityNormalizer:
        public: false
        autowire: true
        autoconfigure: true
        tags:
            - { name: serializer.normalizer }

    trip.api.api_server:
        class: TripBundle\Api\ApiServer
        calls:
            - [ addApiHandler, [ 'accounts', '@TripBundle\Api\AccountsApiInterface' ] ]
            - [ addApiHandler,  [ 'trips', '@TripBundle\Api\TripsApiInterface' ] ]
            - [ addApiHandler,  [ 'countries', '@TripBundle\Api\CountriesApiInterface' ] ]

    trip.model.model_serializer:
        class: TripBundle\Model\ModelSerializer

    trip.service.serializer:
        class: '%trip.serializer%'

    trip.service.validator:
        class: '%trip.validator%'
        arguments:
            - '@validator'

    trip.controller.accounts:
        class: TripBundle\Controller\AccountsController
        calls:
            - [ setSerializer, [ '@trip.service.serializer' ] ]
            - [ setValidator,  [ '@trip.service.validator' ] ]
            - [ setApiServer,  [ '@trip.api.api_server' ] ]
        tags: [ 'controller.service_arguments' ]
        autowire: true

    trip.controller.countries:
        class: TripBundle\Controller\CountriesController
        calls:
            - [ setSerializer, [ '@trip.service.serializer' ] ]
            - [ setValidator,  [ '@trip.service.validator' ] ]
            - [ setApiServer,  [ '@trip.api.api_server' ] ]
        tags: [ 'controller.service_arguments' ]
        autowire: true

    trip.controller.trips:
        class: TripBundle\Controller\TripsController
        calls:
            - [ setSerializer, [ '@trip.service.serializer' ] ]
            - [ setValidator,  [ '@trip.service.validator' ] ]
            - [ setApiServer,  [ '@trip.api.api_server' ] ]
        tags: [ 'controller.service_arguments' ]
        autowire: true


    nelmio_api_doc.describers.config.trip:
        class: Nelmio\ApiDocBundle\Describer\ExternalDocDescriber
        tags: [ { name: nelmio_api_doc.describer, priority: 1000 } ]